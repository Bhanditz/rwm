Please ignore the following warnings which may occur during installation:

no visible binding for global variable '.UserDate'
no visible binding for global variable '.UserDirectory'
no visible binding for global variable '.LastSaved'
no visible binding for global variable '.Describe'
no visible binding for global variable '.WSID'

These variables are defined after the package has been installed and are user
dependent.
